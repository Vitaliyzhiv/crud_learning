RewriteEngine On

# Redirect all requests to public/route.php if the file or directory does not exist
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} !\.(js|css|png|jpg|jpeg|gif|ico|php)$ 
RewriteRule ^(.*)$ route.php [L]
